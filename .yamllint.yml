extends: default
rules:
  line-length: disable
  document-start: disable
  truthy:
    check-keys: false   # GitHub Actions uses a bare `on:` key
  comments:
    min-spaces-from-content: 1
ignore: |
  node_modules/
  target/
